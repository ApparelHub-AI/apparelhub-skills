# ApparelHub Skill

A **portable knowledge package** for AI agents that drive
[ApparelHub.ai](https://apparelhub.ai), the multi-channel ecommerce platform
for custom merchandise (design with AI, then list, sell, and fulfill across
every channel). Use it with Claude Code, Claude Web, ChatGPT,
Gemini, or any agent that can make HTTP requests.

The knowledge core (the 7-phase product pipeline, the field-name
gotchas, the transparency rules, the embroidery thread palette, the
all-over-print rules, the pricing floors, the draft-not-live default,
the mandatory mockup verification) is host-neutral. Optional Claude
Code conveniences live under `claude-code/`.

**This is v2.0, a security-first re-architecture of v1.x.** See
[`SECURITY.md`](./SECURITY.md) for the trust model. v1.x users:
migration notes are at the bottom of this README and in the v2.0
GitHub release notes.

**Who this is for**

- Anyone with an ApparelHub account on the Professional or Enterprise
  tier who wants to drive the platform from an AI agent.
- Claude Code users — see [`porting-guides/claude-code.md`](./porting-guides/claude-code.md).
- ChatGPT Custom GPT / Gemini / claude.ai Projects users — see [`porting-guides/chatgpt-gemini.md`](./porting-guides/chatgpt-gemini.md).
- Agents with only HTTP capability — see [`porting-guides/bare-http.md`](./porting-guides/bare-http.md).

**Quick links**

- Setup walkthrough: <https://apparelhub.ai/agents>
- Browser API docs: <https://apparelhub.ai/developer/api-docs>
- Generate an API key: <https://apparelhub.ai/developer/api-keys>
- Security and trust model: [`SECURITY.md`](./SECURITY.md)

**The agent surface, three ways**

| Repo | What it is |
|---|---|
| **apparelhub-skills** (this repo) | The playbook your agent loads on demand: how to call the API correctly. |
| [**apparelhub-mcp**](https://github.com/ApparelHub-AI/apparelhub-mcp) | The MCP tool server that exposes the pipeline as typed, callable tools. |
| [**apparelhub-recipes**](https://github.com/ApparelHub-AI/apparelhub-recipes) | End-to-end operating blueprints you drop into your agent's runtime to run a whole store pattern. |

---

## What's in this repo

```
apparelhub-skills/
├── SECURITY.md                          # Trust model + non-goals + threat model
├── apparelhub/                          # Host-neutral knowledge package
│   ├── SKILL.md                         # Router, host-neutral
│   ├── BOOTSTRAP-PROMPT.md              # Paste-into-system-prompt scaffold
│   ├── references/
│   │   ├── api-contract.md              # Canonical HTTP contract
│   │   ├── product-creation-pipeline.md # 7-phase workflow detail
│   │   ├── design-rules.md              # AI prompts, transparency, vision verify
│   │   ├── embroidery.md                # Thread palette + sync payload shape
│   │   ├── all-over-print.md            # Pillows, doormats, luggage tags
│   │   ├── garment-catalog.md           # Variant IDs, pricing floors
│   │   ├── orders-and-fulfillment.md    # Order data + payment authority
│   │   ├── product-imagery.md           # Listing images: attach, order, provenance
│   │   └── error-handling.md            # 4xx/5xx semantics + silent failures
│   ├── examples/
│   │   ├── front-print-tee.md           # Bella+Canvas 3001 end-to-end
│   │   ├── all-over-pillow.md           # 18×18 pillow end-to-end
│   │   ├── embroidered-anorak.md        # Champion Anorak chest crest end-to-end
│   │   └── product-imagery-attach-and-order.md  # Add a photo to a live listing
│   └── scripts/                         # Optional Claude Code conveniences
│       ├── ah_check                     # Auth precondition: verify key works
│       ├── ah_poll_mockup               # Two-phase mockup wait state machine
│       ├── ah_classify_previews         # Local preview-row parser
│       ├── ah_pick_provider_url         # Extract one preview URL by color+angle
│       ├── ah_pick_dimensions           # Compute print dimensions from design
│       └── make_transparent.py          # Chroma key + auto-crop
├── porting-guides/                      # Per-platform install/usage walkthroughs
│   ├── bare-http.md
│   ├── claude-code.md
│   └── chatgpt-gemini.md
├── claude-code/                         # Claude-Code-specific overlay
│   ├── README.md
│   ├── settings.recommended.json        # Optional permission allowlist
│   └── install_path.sh                  # PATH editor used by install.sh
├── acceptance/
│   └── playbook.md                      # Pre-release verification
├── .github/workflows/
│   └── forbidden-patterns.yml           # CI guard for v1.x antipatterns
└── install.sh                           # Claude Code installer
```

The knowledge in `apparelhub/` is what any agent reads. Everything
else is platform-specific scaffolding.

---

## Install

Pick the path that matches your agent.

### Hosted connector for Claude.ai, Perplexity, ChatGPT (no install)

The lowest-friction path, with nothing to install. In Claude.ai or Perplexity (or ChatGPT on Business or Enterprise), add a **custom connector** pointing at:

```
https://mcp.apparelhub.ai
```

Sign in to ApparelHub once and the connector links to your account. Your agent gets the full ApparelHub tool surface (design, build, sync, orders) as typed tools, run on our compute. No key to paste, no shell. Walkthrough: [Use ApparelHub from Claude on the web](https://apparelhub.ai/blog/use-apparelhub-from-claude-web).

The rest of this section covers running the skill or tools inside your own agent runtime.

### Claude Code (CLI on macOS/Linux)

```bash
curl -fsSL https://apparelhub.ai/install-skill.sh | bash
```

The installer:

1. Clones the repo to `~/.apparelhub-skills/`.
2. Symlinks `~/.apparelhub-skills/apparelhub/` into `~/.claude/skills/apparelhub/`.
3. Adds the skill's `scripts/` directory to your shell PATH (bash, zsh, fish).
4. Prompts for your `APPARELHUB_API_KEY` (or reads it from env), verifies it against the platform with `ah_check`, then exits.
5. **Does not persist the key to disk by default** — that changed in v2.0 (see [`SECURITY.md`](./SECURITY.md)). Put `export APPARELHUB_API_KEY=...` wherever you manage development secrets: shell rc, direnv, macOS Keychain, 1Password CLI, etc.

If you want the v1.x persistent-`.env` behavior, pass `--persist`:

```bash
curl -fsSL https://apparelhub.ai/install-skill.sh | bash -s -- --persist
```

You'll see a warning explaining the trade-off before the write happens.

Inspect the script before piping to bash:

```bash
curl -fsSL https://apparelhub.ai/install-skill.sh | less
```

Source on GitHub: <https://github.com/ApparelHub-AI/apparelhub-skills/blob/main/install.sh>

Then follow [`porting-guides/claude-code.md`](./porting-guides/claude-code.md).

### ChatGPT Custom GPT / Gemini / claude.ai Projects

Two paths in [`porting-guides/chatgpt-gemini.md`](./porting-guides/chatgpt-gemini.md):

- **Path A** — paste [`apparelhub/SKILL.md`](./apparelhub/SKILL.md) (or [`apparelhub/BOOTSTRAP-PROMPT.md`](./apparelhub/BOOTSTRAP-PROMPT.md) for a short version) into the agent's system prompt, attach the `references/` files as knowledge files.
- **Path B** — register a single `apparelhub_request(method, path, body)` function that hides the canonical host and the API key inside the implementation. The agent can compose the pipeline without ever seeing the raw key. Highest-confidence path for production use.

### Bare-HTTP agent

Read [`porting-guides/bare-http.md`](./porting-guides/bare-http.md).
Same flow, no shell, no scripts, no filesystem. Multipart-from-disk
or JSON data-URL — your choice. The HTTP contract is in
[`apparelhub/references/api-contract.md`](./apparelhub/references/api-contract.md).

---

## Optional — Claude Code permission allowlist

The repo ships [`claude-code/settings.recommended.json`](./claude-code/settings.recommended.json)
with patterns that allowlist the helper scripts plus the canonical
`https://api.apparelhub.ai` curl prefix. Merge it into your
`~/.claude/settings.json` to drop a few prompts on routine calls.

**Note.** Claude Code will still prompt the first time the agent
expands `$APPARELHUB_API_KEY` even with the allowlist in place. That's
the safety control working correctly. Approve in context if the call is
one you intended. The skill no longer ships wrappers whose purpose is
to dodge those prompts; see [`SECURITY.md`](./SECURITY.md) §2a.

---

## What changed in v2.0

The full migration guide is in the v2.0 GitHub release notes. The
short version:

- `ah_curl` is deleted. It forwarded the API key to arbitrary URLs.
  Replace any of your own scripts that called `ah_curl GET /...` with
  plain `curl https://api.apparelhub.ai/agents/v1/...` invocations.
- `APPARELHUB_API_BASE` is no longer read by helpers or the installer.
  The canonical host `https://api.apparelhub.ai` is hard-pinned.
  Internal contributors targeting dev should fork and edit the constant
  at the top of the script.
- The installer no longer writes `~/.apparelhub-skills/.env` by default
  and no longer edits your shell rc. Manage `APPARELHUB_API_KEY` via
  your host's secret manager.
- The Claude Code-specific bits (`settings.recommended.json`, the PATH
  helper) moved into a `claude-code/` overlay.

**Migration for v1.x users:**

1. Remove the v1.x rc-file sourcing line `[ -f ~/.apparelhub-skills/.env ] && . ~/.apparelhub-skills/.env` (or `source ~/.apparelhub-skills/.env.fish`).
2. `rm -f ~/.apparelhub-skills/.env ~/.apparelhub-skills/.env.fish`.
3. Put `export APPARELHUB_API_KEY=...` in wherever you manage dev secrets.
4. If you suspect the v1.x `.env` was ever exposed, rotate the key at <https://apparelhub.ai/developer/api-keys>.
5. Re-run the installer: `curl -fsSL https://apparelhub.ai/install-skill.sh | bash`.
6. Update your `~/.claude/settings.json` to use the v2.0 patterns in [`claude-code/settings.recommended.json`](./claude-code/settings.recommended.json) (drop the `Bash(ah_curl ...)` entries).
7. Replace any `ah_curl` invocations in your own scripts with plain `curl https://api.apparelhub.ai/agents/v1/...` calls.

---

## Versioning

| Version | Date | Summary |
|---|---|---|
| 2.61 | 2026-08-27 | **The skill told an agent to hand-roll a listing photo out of the generic img2img path, which applies no preset and enforces no refusal.** `references/product-imagery.md` §6b now documents the dedicated path: `POST /images/generate` with `source_product_uuid` plus a `style` preset (`on_model`, `detail`, `flat_lay`, `lifestyle`), which resolves the product's best real mockup itself and applies preset text written to hold the product fixed while changing only its surroundings. Fetch the menu from `GET /product/listing-image-styles` rather than hardcoding it — the preset text IS the safety property, so a stale local copy can drift away from "edit the real mockup". ⛔ **Do not send a `prompt` to satisfy a validator**: the preset is the prompt, and anything you send is folded in as EXTRA GUIDANCE ahead of the fidelity clause, so filler does not get ignored, it steers the generation. ⛔ **A product with no mockup is refused rather than generated from scratch** — that is the constraint working, so render a mockup and ask again; raw print artwork does not count as one. MCP callers get the new `generate_listing_image` tool, which wraps this, restricts model fallback to edit-capable models, and attaches only on `attach: true` (via the APPEND endpoint, so it cannot clobber a gallery the way a replace would). Notes that this spends an image generation and that four styles across thirty products is 120 of them, more than some plans allow in total. Docs only. |
| 2.60 | 2026-08-27 | **The skill covered listing images only at creation time, so an agent could not attach a merchant's photo to a product that already existed, reorder a gallery, or change a cover — and had no way to know that ordering decides what actually ships.** New `references/product-imagery.md` plus a worked example, and `references/api-contract.md` gains the request/response shape for `PATCH /product/{uuid}` (previously one sentence naming two fields with no body, no ordering semantics and no error). Leads on the expensive confusion: a **print file** is the artwork that gets PRINTED, a **listing image** is what a shopper sees, and pushing artwork as listing photography is a known quality failure on at least one major channel — the platform marks it `source: "print_file"` and excludes it from channel sync unless it is the only image there is, which is a safety net rather than a plan. ⛔ **`gallery_images` REPLACES the gallery, it is not a merge**, so adding one photo to a product with four means reading `images` and sending all five; sending only the new URL deletes the rest. ⛔ **Ordering is functional, not cosmetic**: channels cap listing images and truncate in gallery order (TikTok Shop 9, Wix 15, Shopify and WooCommerce uncapped), so an agent appending new images to the end has decided without meaning to that they do not exist on the capped channel. Generated imagery is constrained to EDITING the product's real mockup, and the section says why plainly — a from-scratch product photo invents a garment that does not exist, which is a takedown and a chargeback rather than a quality problem. `ai_generated` is a tri-state declaration independent of `source` (an uploaded photo may itself be AI-made and the platform cannot detect it), `null` means unknown rather than no, and it should be set truthfully because some channels have synthetic-media disclosure rules. Concurrent writes get `images_version` / `expected_images_version`, with the new `images_version_conflict` 409 in `references/error-handling.md` §2 — on conflict, reapply your intent to the CURRENT gallery rather than retrying a stale array. Also wires up `ah_classify_previews --recommend`, whose `{display_image, gallery_images}` output has always been exactly the PATCH field names and was consumed by nothing. Docs only. |
| 2.59 | 2026-08-26 | **Repository documentation only. The skill package under `apparelhub/` is byte-identical to 2.58, so there is nothing to re-install and no agent behaviour changes.** The README surface overview now names all three agent surfaces rather than two: this repo is the playbook, [`apparelhub-mcp`](https://github.com/ApparelHub-AI/apparelhub-mcp) is the typed tool server, and [`apparelhub-recipes`](https://github.com/ApparelHub-AI/apparelhub-recipes) holds end-to-end operating blueprints you drop into an agent's runtime. Recipes had no inbound link from here, so a reader landing on the skill had no path to it. |
| 2.58 | 2026-08-25 | **Connection health could not answer at all for the most-used fulfillment provider, and the skill listed reason codes an agent would now hit and not recognise.** The endpoint used to probe every provider by spending a Personal Access Token, which only exists on PAT providers, so Printful (OAuth) returned a raw internal error. It now asks each provider the question it can answer: PAT credentials are spent, OAuth credentials are checked by exercising the **refresh** token, since an OAuth access token expires hourly and its expiry proves nothing. `references/error-handling.md` §2f gains the two new reason codes. **`reconnect_required`** is the one retrying can never fix: it carries `remedy: "reauthorize"`, and the merchant goes back through the connect flow — ⛔ do not ask them to paste a token, OAuth providers do not use one. **`health_check_unsupported`** arrives with `healthy: null` and means we could not probe that provider at all, which is not evidence about their credential. Also documents a side effect worth knowing: checking health on an OAuth connection near expiry **refreshes and persists a new access token**, so this endpoint is not strictly read-only and should not be polled in a loop. Docs only. |
| 2.57 | 2026-08-25 | **Connecting a fulfillment provider fails for five reasons and only two of them are the credential's fault.** The platform used to report all five as one sentence telling the merchant to check their token, and a real merchant replaced a working credential and gave up mid-setup. `references/error-handling.md` gains **§2f** with the full table: `invalid_token` and `insufficient_scope` are the merchant's to fix, while `rate_limited`, `provider_unavailable` and `transport_error` say nothing at all about the credential. ⛔ **Branch on `credential_at_fault`, not on the HTTP status**, and never ask for a new credential when it is false — that is how you send someone to replace the one thing that was working. `insufficient_scope` in particular needs a *differently scoped* token, not merely a new one, or they will create the same broken credential again. Also: **429 is now two different things** and the §1 table says which. A body carrying `error: provider_rate_limited` means the FULFILLMENT PROVIDER is throttling us and your key is fine, so back off the request rather than your key. And **connection health is a tri-state** — `healthy: null` means we could not determine it, and prompting a reconnect on `null` is a false alarm the endpoint used to generate every time a provider had a blip. Docs only. |
| 2.56 | 2026-08-21 | **Placed designs are prepared for print automatically now, and flagged when they are not.** A text-to-image generation intended for a placed print is keyed to transparency server side, at generation, and reports what it did in a `print_ready` block on the generate response and the status poll. ⛔ **Do not key a design twice** — one that came back `applied` will refuse `process_transparency` with `already_transparent`. `process_transparency` still applies to designs you UPLOADED, designs made before this shipped, and designs where the key was refused, so read the block rather than assuming either way. More useful day to day: the mockup poll and the preview-jobs listing now answer *"will this print as a rectangle"* with a **`design_check`** block, and `ship_product` carries it through as a warning so an unattended run sees it BEFORE the product is created. **Any** opaque background on a placed print prints as a solid rectangle, not only an un-keyed green screen, and it is the most common way a product ships visibly broken. Gated to **placed** prints, so a full-bleed design is never flagged and must not be "fixed". Also documents choosing a background colour that CONTRASTS with the subject: a green dragon on a green screen keys the dragon away, which is the failure a single fixed colour always had waiting in it. Docs only. |
| 2.55 | 2026-08-18 | **An order the sales channel is reviewing is now HELD before fulfillment, and the skill would have told an agent to do the one thing that cannot work.** Channels sometimes hold an order for their own risk review and can still void it; the platform now ingests that order normally and holds it at the fulfillment boundary, so nobody pays to print a garment for a sale that disappears. `references/orders-and-fulfillment.md` documents `hold_channel_risk_review` (**ON by default**) in its own section, because it does NOT behave like the guardrails beside it: it **overrides `fulfillment_mode`, including `auto`**. An agent carrying the usual precedence would conclude an auto-pilot store ships these orders, try to clear the hold by flipping the mode, and be wrong twice. ⛔ The setting to change is the rule itself, and that is the merchant's call, not the agent's. The usual next step is to **wait** rather than decide — unlike `high_value` or `low_margin`, this hold typically clears itself when the channel finishes reviewing, and reconciling pulls the channel's current state on demand. Also states the order is real and **paid** (the buyer WAS charged; only fulfillment is held) so the hold is not misread as a payment problem. While here: `hold_first_time_customer` was missing from the guardrail list entirely, and `first_time_customer` / `channel_risk_review` were missing from the `hold_reason` values an agent matches on — an agent cannot branch on a reason code it has never been told exists. Docs only. |
| 2.54 | 2026-08-12 | **Adding variants to a product that was already synced looked finished the moment `add_variants` returned — and it isn't, so an agent could add a size, see success everywhere, and leave orders for it unable to reach the provider.** A new variant starts with no fulfillment link; until a re-sync stamps it, the webhook that ingests a paid order **silently drops that line** and manual order/estimate creation rejects it, while the listing looks perfectly live. `references/product-creation-pipeline.md` (Phase 5) and `references/error-handling.md` (§3) now say: after adding variants to an already-synced product, **re-run the fulfillment sync**, then confirm each variant carries a non-null `provider_external_id`. Also names the trap that made the original report confusing — the variant-level `provider_external_id`/`external_id` is the FULFILLMENT link, which only collides in NAME with the channel-level `external_id` under `ecommerce_statuses[]` (the storefront LISTING id), so a null variant link says nothing about the channel. And documents `classification_basis` on a variant sync: TikTok Shop and Wix edit the whole listing at once and report `channel_listing_snapshot`, whose `added`/`updated` is a pre-sync diff (a variant a prior whole-product sync already pushed reads as `updated`, not `added`), so confirm a new variant reached those channels by re-reading the listing — Shopify and WooCommerce report the authoritative `per_variant_api`. Docs only. |
| 2.53 | 2026-08-12 | **The skill explained a missing cost with a mechanism that isn't how cost arrives, and its own provider-comparison step could never be completed for one of the three providers.** Cost was described as "populated by the fulfillment sync" in five places; syncing to fulfillment does not fill it in, so an agent following that would sync, see cost still empty, and have nothing to try next. Cost is resolved when the **variant is created**: Printful and Gelato publish it in their catalogs, and Printify does not publish it at all but reports it once the product exists on their side, which is what **generating a mockup** does. So the normal build order already produces real Printify costs with nobody retyping anything, and the old advice to look a cost up in the provider's catalog and enter it by hand is gone. Catalog cost now carries **`cost_source`** (`live` = read from the provider this request, `cached` = a snapshot from the last build here with `cost_captured_at` for its age, `unavailable` = genuinely unknown), and `references/provider-selection.md` gains a comparison strategy built on it. ⛔ **Never present a cost comparison with a provider silently missing from it** — a table with a quiet gap reads as complete and gets acted on as though it were; either build the candidate to obtain the number, or say which one you could not get. Also states the two limits to repeat whenever quoting a comparison: this is **base production cost** excluding shipping and tax, and `estimate_order_costs` covers Printful and Gelato but **not** Printify, so a true landed comparison across all three is not something an agent can produce today. Docs only. |
| 2.52 | 2026-08-12 | **The skill told an agent a missing size chart was nothing to do about — and US apparel is graded down without one.** A chart is normally rendered automatically from the fulfillment provider's per-size measurements, but only ONE of the three providers publishes them, so a listing built on a blank from either of the others gets no chart at all. `references/listing-attributes.md` gains a **Size charts** section and `product-creation-pipeline.md` stops saying there is nothing to do. The remedy is now agent-drivable: `import_size_measurements` returns the provider's real table in exactly the shape `size_chart_measurements` takes, so it goes straight back through `set_listing_attributes`. ⛔ **Prefer the measurements over `size_chart_template_id`** — a template id can only come from a human working in the channel's own admin, because the channel publishes no way to LIST templates, so an agent can never discover one, verify that an existing one is right, or correct a wrong one. ⛔ **Never invent measurements**: not from the garment type, not copied from a similar product, not a plausible number in a gap. A malformed table is refused whole rather than half-applied, a missing cell renders blank and is fine, and an invented one means somebody receives a garment that does not fit — worse than no chart. `available: false` from the import is an ANSWER with a reason (the provider has no size-guide API at all, versus none for this particular blank, versus a transient outage), and the `source` names which provider and catalog item the numbers came from, because they are a claim about a physical garment published under the merchant's name. Also re-scopes the size chart to per-PRODUCT (it describes the blank, like brand) and notes that an `object` field publishes its shape in `channel_ref.object_schema`. Docs only. |
| 2.51 | 2026-08-11 | **Brand moved from shop-wide to per-product, and the skill still said the old thing — an agent following it would call `set_channel_settings` for a field that no longer lives there.** Brand is a property of the BLANK, so a shop selling Comfort Colors tees and Gildan hoodies has two brands and one shop-wide value cannot express that. `references/listing-attributes.md` re-scopes it and adds the part that saves work: the platform now reads the manufacturer off the fulfillment provider and resolves it to the channel's brand id automatically, so an unset brand usually fills itself in on the next sync rather than needing an agent to reach for one. ⛔ A brand that does NOT resolve is deliberate, not a failure — channel registries are full of near-identical entries (one real search for `Gildan` returns 147 results including `GILDAN PLATINUM` and `Jerzees or Gildan`), so the match is exact or nothing. An unset brand is honest; a wrong one is a false claim about who manufactured the merchant's goods. Docs only. |
| 2.50 | 2026-08-11 | **A successful sync is a fact about the past, and the skill had no way to say so — an agent would read `sync_status: Synced` and report a listing as live when the channel had already taken it down.** `references/product-creation-pipeline.md` gains **Phase 9**: read `health` (what the CHANNEL last said) rather than `sync_status` (what we believe we published), and when they disagree the channel wins. The distinctions that matter are tabled: `Removed` is gone, `Needs Attention` is present but invisible to buyers, `Unknown` means we could not reach the channel and is **not** a claim that anything is wrong, and an absent `health` means NEVER CHECKED — which is not the same as healthy, and is the one most likely to be reported as "fine". ⛔ The operative rule: do not immediately re-sync a `Removed` listing. The channel removed it for a reason, pushing it back unchanged tends to get it removed again, and repeated churn on one product is itself a risk to the shop — fix the flagged problem first. `health_reason` carries the channel's own explanation when it gave one and is often the only actionable detail, because it arrives on the channel's push notification and cannot be recovered by polling later. Also notes `external_id` is always a string: channel product ids can exceed 2^53 and corrupt if parsed as numbers. Docs only. |
| 2.49 | 2026-08-11 | **Every sales channel defines extra fields on a listing — product attributes, item specifics, category metafields — and an agent had no way to learn what they were, so it could only blind-write a guess and watch it silently disappear.** New `references/listing-attributes.md` documents the describe → set → sync loop, and leads on the two things that make the difference between setting a field and thinking you did: a value is stored against the listing and does **not** reach the channel until a sync, and a refused value comes back in `rejected` with a machine-readable reason rather than vanishing. The schema is read with three properties kept deliberately separate — `value_type`, `cardinality` and `free_text` — because most real fields are enumerated AND accept free text at once, so neither flag alone says what is legal. Two traps get their own treatment: `values` reports what is LIVE on the channel rather than what was last written from here (channel-side auto-fills and merchant edits show up there, and that drift is the useful part), and a `keyword_match` category is a GUESS — if it is wrong the fields belong to a different kind of product and setting them is worse than setting none. ⛔ §4 is the one that matters most: compliance answers are LEGAL ATTESTATIONS made in the merchant's name. Relay what they told you, never choose a value, do not reason that printed apparel must be exempt, and if they have not answered leave it unset and say so. Docs only. |
| 2.48 | 2026-08-11 | **The grid could tell you what to fix and never whether the fix worked, so an agent editing a title and seeing a different number a week later could only claim credit or learn nothing.** `references/channel-performance.md` gains §8 for `/agents/v1/analytics/interventions`: every shopper-visible edit is recorded as it is made, with the signal state that prompted it, and gets a verdict on the ONE metric that change should have moved (a title is judged on click-through, not revenue — judging it on revenue imports every confound in the shop). The section leads on the trap: **`unmeasurable` is the DEFAULT verdict, not an error, and it does not mean the change had no effect** — on a low-traffic shop it will be most of them, and saying "your change did nothing" when the truth is "nobody saw the listing either way" is the §3 mistake one step later in the process. `verdict_reason` is tabled so an agent knows whether to wait, reconnect, or stop recommending edits entirely, and `confounded` is called out as a refusal to guess rather than a gap. Also §7: the grid now defaults to listings a shopper can actually buy — a deactivated, fulfillment-unmapped product still carries a channel record, and on a real shop 17 of 38 rows were unactionable advice sitting above the ones that mattered. Docs only. |
| 2.47 | 2026-08-11 | **A listing was being called `dead` because the SHOP had no traffic — on a shop whose best listing managed 26 views in a month, 8 of 22 listings were offered as safe to archive.** The archive test used an absolute impression floor and ran before any store-relative reasoning, so the shop could not judge a single listing yet volunteered eight of them for deletion. `references/channel-performance.md` gains a new §3 that puts the SHOP verdict (`summary.shop`) ahead of every per-listing state: when it reads `no_channel_traffic`, no listing state means anything yet, and the honest finding is a distribution problem — editing a title cannot help a listing nobody is shown. Also adds the seventh state, `no_channel_data`: products synced to a channel that has never reported them (not even a zero) used to be silently absent from the grid, so a merchant with 37 synced products saw 22 rows and read that as their catalogue. Its metrics come back absent rather than zeroed, and it routes to checking the listing is live — nearly always an approval or stock problem that neither archiving nor a rewrite touches. Docs only. |
| 2.46 | 2026-08-11 | **A channel product id is only unique within its own channel, so a row read without its channel is a conclusion with no basis behind it.** Channel-performance rows now carry `provider` / `store_name` / `store_uuid`, and both endpoints take `provider` and `store` filters and return `channels_present` (the channels that actually have data in the range, so a filter can never be set to something that returns nothing). New `references/channel-performance.md` §2b states the rule outright: always read a row with its channel, because two unrelated listings on two channels can share the same id. Filtering narrows the summary counts too, so `summary` and `listings` never disagree. Docs only. |
| 2.45 | 2026-08-11 | **Order-based analytics only know what SOLD, so a listing hundreds of people viewed and nobody bought looked identical to one nobody had ever seen — both read as "0 sold", and they need opposite actions.** New `references/channel-performance.md` covers the three `/agents/v1/analytics/channel/*` endpoints that serve what the sales channel itself reports about each listing (impressions, clicks, CTR, add-to-cart), plus a derived state per listing and the fix each one implies. Two of those states are easy to get wrong and are called out: `starved` (too few people have seen it to judge) is a DISCOVERY problem, not a reason to rewrite the listing; and `dead` is the only state where archiving is appropriate — `conversion_blocked` is its opposite, proven demand with a broken listing, and archiving it destroys the best opportunity in the catalogue. Also states the rule that a missing metric is never a zero (WooCommerce core records no view data at all, so absence means "not reported", not "nobody looked"), that channel dates are on a different clock from the order analytics, and that recent days are provisional because channels restate. Cross-linked from `analytics.md`. Docs only. |
| 2.44 | 2026-08-11 | **A model refusing a design on content grounds was being treated as the end of it.** The guidance said to revise the prompt, so agents reworded and then gave up: in one real session an agent hit a block, reworded the same idea six times, was refused every time, and abandoned the design while other models sat untried. That is the wrong lever, because these guards react to subject matter more than to phrasing, and they are **provider-specific** — some run a recitation/copyright guard, others only screen for unsafe content, so a prompt one provider refuses another routinely renders with no objection. New `references/error-handling.md` §2e.6 states the protocol plainly: reword at most twice (varying the motif and palette, not the adjectives), then let the model sweep run, and abandon only on `content_blocked_all_models` — the one code that means every model refused, and the only legitimate grounds to give up on a design. A single `content_blocked` never is. Also covers what a content block is (a normal outcome for some subject matter, not a platform fault, nothing to escalate) and notes that a design which only rendered after a block was routed around is worth a compliance look before it goes on a product. Cross-linked from `design-rules.md` §4. Docs only. |
| 2.43 | 2026-08-10 | **TikTok flags a title as too short and then offers no replacement — on a real catalog that was 27 flagged and 0 suggested, so an agent asking to apply a title got `no_recommendation` every time.** Each diagnosed listing now also carries `requirements` (the computed target — TikTok's own length rules contradict each other, and this is the intersection that satisfies all of them), `building_blocks` (the product's real garment, colors and sizes, so a title is written from facts rather than invented), and `candidates.title` (ready-to-use options, shortest first, each pre-validated). New `issues[].fixable_by` says whether a problem is even yours: `photography` means the listing needs imagery, not better writing. Documents one honesty caveat — TikTok has flagged a title that literally contains "T-Shirt" for lacking a product word, so length is guaranteed and the subject-word rule is not. `references/product-creation-pipeline.md` Phase 8. Docs only. |
| 2.42 | 2026-08-10 | **A listing could be complete and still be graded POOR, and nothing said so.** TikTok grades every listing POOR/FAIR/GOOD and a low grade suppresses how often it is shown, but an agent had no way to read that grade or the reasons behind it. New `diagnose_tiktok_listings` returns, per listing, the current tier, the machine-readable issues (`code` + `how_to_solve` + the tier that ONE fix unlocks), and TikTok's own recommended search terms and title/description. Passing `apply` writes those recommendations back and re-syncs; `dry_run` previews first. `search_terms` is the default because it is hidden metadata, while `title`/`description` replace merchant-visible copy and should be confirmed with the user. Those two land on new TikTok-ONLY `tiktok_listing.title` / `.description` overrides, so applying TikTok-tuned copy cannot rewrite the same product's Shopify/WooCommerce/Wix listings. Also documents the two things that keep a report honest: only LIVE listings are gradeable, and the tier does not move the instant an edit lands because TikTok re-grades asynchronously. `references/product-creation-pipeline.md` Phase 8. Docs only. |
| 2.41 | 2026-08-06 | **TikTok listings were shipping incomplete: no search terms, no highlights, the wrong category, no size chart, and a shipping rate that read as free.** `update_product` now takes an optional `tiktok_listing` object so an agent can supply the TikTok-specific fields the platform now accepts: `search_terms`, `key_product_features`, `brand_id`, category `attributes`, `weight`, `dimensions`, and `shipping_template_id`. Unknown keys are rejected before any write. Category resolution now maps a plain tee to the real apparel leaf (e.g. Men's Short-sleeve T-shirts) instead of a lookalike like golf tees. Size charts auto-generate for apparel from the fulfillment provider's measurements, so an agent never has to build one; compliance attestations stay merchant-set and are never invented by the agent. `references/product-creation-pipeline.md` Phase 8 documents all of it. Docs only. |
| 2.40 | 2026-08-04 | **A store the caller could plainly see was refused as "not found", and the sentence sent them after credentials that were never read.** On a multi-workspace account, omitting `?workspace=` does not mean "wherever the store is" — it means Default, so a store in any other workspace is simply unreachable. That bites hardest on the store-scoped setup calls, because the store is resolved *before* any provider call: `connect-api-key` and `initiate` fail identically whether the credential is correct or mistyped, and the readiness / connection-status polls answer for the wrong workspace and report "not connected" for as long as you keep polling. A new `409 wrong_workspace` names the workspace and hands back a `retry_with`, documented in `references/error-handling.md` §2b; `references/workspaces.md` §2 now states that omitting the param is itself a failure mode rather than a safe default, and that store-scoped setup calls need it just as much as lists do. Also corrects a stale claim that no agent endpoint lists workspaces — `GET /agents/v1/workspaces` does. Docs only. |
| 2.39 | 2026-08-03 | **Some accounts cannot be upgraded through the API at all, and saying otherwise sends a merchant somewhere they can do nothing.** Merchants who installed from the Shopify App Store pay through Shopify's own hosted plan page, so plan selection is not a call an agent can make for them. `GET /agents/v1/membership` now carries `billing_provider` and a `billing` block, and `GET /agents/v1/membership/billing-route` resolves where a payment action must go; a `provider: "shopify"` route means hand the merchant the `checkout_url` and stop. A null `checkout_url` still means Shopify, never a fallback to a payment page on apparelhub.ai. The 402 `account_suspended` body carries `billing_provider` too and points at the Shopify admin for those accounts, so quoting its `message` and `billing_url` stays correct on both providers. `sellable_by` per tier says which provider can actually charge for a plan, so a tier the merchant's provider cannot sell is not offered. Docs only. |
| 2.38 | 2026-08-03 | **A capability limit belongs to a provider, not to a category of garment.** An agent found one provider's headwear was embroidery-only and reported that no hat was possible — while the same account carried printed caps that take photoreal artwork as-is. Every garment now reports `decoration_method` / `accepts_photoreal` (with `null` meaning *nobody could tell*, never *cannot*), and `find_garments` searches **every provider on the account by default** so checking is cheaper than not checking. New `references/provider-selection.md` + `examples/blocked-garment-find-alternative.md`; rail 4e is now provider-neutral; new rail **4g** — never state a capability limit you have only checked on one provider. Headwear is no longer pre-classified as embroidery in the decision tree, because pre-classifying it is what made a printable cap invisible. |
| 2.37 | 2026-08-03 | **Knitted garments: the design is reproduced in yarn, not printed** (`references/error-handling.md` §2e). A knitted garment has no ink — the artwork is knitted in yarn — so the provider refuses to render one until told which yarns to use, and a merchant who picked one from the catalog previously got raw provider jargon naming no option and no next step. Yarn colours are now matched to the design automatically and the preview SUCCEEDS with a note naming the yarns chosen; `knit_options_unavailable` only appears when no usable colour can be read, and then it carries the full yarn palette and the colour cap so a caller can pick. The design rule is the part that bites and is now stated plainly: a knit render **quantises artwork to the yarn colours**, exactly as embroidery quantises to thread — so gradients and fine detail do not survive, and a single-dominant-colour design comes back as a flat block with the call still reporting success. Docs only. |
| 2.36 | 2026-08-03 | **Four preview and generation dead-ends now say what to do next** (`references/error-handling.md` §2e). Each previously handed an agent something it could not act on, so an unattended run either retried the identical request forever or gave up. `variant_ids: []` was read as "all variants" — it fanned out to a 30-variant provider render and returned a bare 500; it is now rejected as `empty_variant_selection` with the available colours and sizes to re-pick from. A placement set the provider refuses is now reduced automatically where possible, returning `warnings[]` naming what was left off (**a success, not a failure**), and where it cannot be reduced it returns `placement_constraint` carrying the requested and attempted placements plus the cap. A failed image generation now returns a machine-readable `error_code`, and the docs are explicit that its three values mean three DIFFERENT remedies — revise, retry, rephrase — because `content_blocked` is the only one where retrying is pure waste, and sending a merchant off to fix a prompt that was never the problem is worse than saying nothing. Docs only. |
| 2.35 | 2026-07-31 | **SVG uploads, and the two ways a vector mark silently loses part of itself** (`references/byo-artwork.md` §2, `error-handling.md`). Vector is now accepted and is the *preferred* input for a logo: it is rendered server-side at print resolution, so it stays crisp at any print size where a raster of the same mark could only be enlarged. Two conditions, both refused with instructions rather than silently mangled — **text must be converted to outlines** (rendering runs with no fonts on purpose, because substituting a font in a brand wordmark is corruption, not a fallback; live text would come out blank, or a logo would come out with its mark intact and its wordmark quietly gone), and **linked images must be embedded** (a URL reference is never fetched, so it renders as a hole). Both are PARTIAL losses that look like a successful upload, which is why they get their own error codes rather than a generic failure. AI/EPS still need exporting first, and the docs are explicit that exporting is not redrawing. Docs only. |
| 2.34 | 2026-07-31 | **Bring your own artwork — the merchant already owns the file** (new `references/byo-artwork.md`, plus `product-creation-pipeline.md` Phase 1, `error-handling.md` 2a-bis, SKILL.md routing). The skill only ever taught one way to get a design: generate it. An agent handed a client's own logo, wordmark and cleared covers — with an explicit instruction not to redraw them — found nothing about uploading, concluded the work was impossible and stopped. Uploading was supported the whole time. The new reference covers the three routes (public `source_url`; a presigned URL you PUT to yourself, which costs nothing in context and is the right default whenever you can make an HTTP request; inline base64, which costs roughly **350k tokens per megabyte** and is the last resort), the rasterize-don't-redraw answer for vector sources, and the `upscale: "pixel"` control that stops a small hard-edge mark being smoothed into mush when it is enlarged to the print minimum. Uploads are storage-metered and consume **no** image generations. States the rule plainly: never regenerate, redraw or approximate a mark you were given as a file — if you are blocked, say what you need. Docs only. |
| 2.33 | 2026-07-30 | **Filter designs by model name, and stop trusting two quality signals that could not be trusted** (`references/design-rules.md`, `api-contract.md`, `error-handling.md`). The designs listing accepted `source` and silently ignored it, so a filtered-looking `200` was the full unfiltered list; it now takes the AI source NAME — the same form `POST /images/generate` takes — and an unknown name is a **400** with the valid list. Two QC signals get honest semantics: `verify_design_quality` now BLOCKS on a green chroma halo and on a solid black-box artifact (it previously measured only alpha, resolution and premultiply, and returned **100/100** on a design carrying both — and unlike the resolution warn, nothing downstream removes these), and `text_verified.has_text` returns **`null` for UNKNOWN** when OCR is unavailable rather than `false`, which had agents skipping the spelling check on designs covered in text. Docs only. |
| 2.32 | 2026-07-29 | **All-over print: read `warnings`, and check the BACK render** (`references/all-over-print.md` §10). A fill build could report `print_style: fill`, every placement covered, full-bleed geometry and an empty `warnings` array while shipping a patterned front with a **blank back and blank sleeves** — and those blank files reached the fulfillment provider. The platform now checks composed pixels and reports a single-colour print file or one file reused across different print areas; treat either as ship-blocking. Also: never pad/letterbox a design destined for all-over print — the flat bars print as an unpatterned band at the hem, and cover-fit cannot remove them once the padded frame matches the area aspect. Docs only. |
| 2.31 | 2026-07-29 | **Designs can be archived and deleted, and orphans are findable (`references/design-rules.md` new section 5d).** An agent auditing a gallery concluded "no design delete or archive endpoint exists in the entire agent API" and skipped the cleanup. All three primitives existed; the docs hid them. Archive is a **PATCH** on `/images/generated/{uuid}` with `{"archived": true}` (reversible, safe, never touches products using the design) — easy to miss because it is not a dedicated endpoint, and the PATCH accepts **only** `title` and `archived` (other fields are ignored and still return `200`). `DELETE` is permanent and is refused with **`409 image_in_use`** while a live product uses it, listing the blocking products; archive instead. Orphan designs have a purpose-built filter: `GET /images/generated?on_products=false`. `api-contract.md` gains the PATCH/DELETE rows and the full listing filter set; `error-handling.md` decodes `image_in_use`. Docs only. |
| 2.30 | 2026-07-29 | **Catalog search actually filters now — and a thin result is a prompt to widen, not a verdict** (`references/garment-catalog.md` new *Searching the catalog* section). `category` / `keyword` / `per_page` on the catalog listing used to be accepted and silently ignored, so a "narrow" query returned the full catalog and agents reasoned from the dump. They are honoured now: `category` resolves against each provider's own taxonomy and an unknown one returns **400** with the valid list. Filtered calls return `{garments, total, filters_applied, warnings}` — **read `warnings`**: `category_matched: "kids / Hats"` means the term resolved inside ONE department, so one product does not mean one product exists. `has_aop` is accepted but ignored and says so. Docs only. |
| 2.29 | 2026-07-20 | **Flux 1.1 Pro is NOT an editor (design-rules §5c correction).** Flux 1.1 Pro's img2img is Flux Redux — a composition/style *variation*, not an instruction-editor (it won't "add a star" / "make it blue") — so it is no longer offered for editing; it stays a text-to-image model. The two non-editors are now `Google Imagen 4` (text-only) and `Flux 1.1 Pro` (Redux); an edit request on either returns a clean **400**. Everything else (Nano Banana, OpenAI, GPT Image 2, Seedream 4.0/4.5, Flux 2 Pro, Grok Imagine, Wan 2.7) is a true editor. Docs only. |
| 2.28 | 2026-07-20 | **img2img edit now works on almost every model, not just Nano Banana/OpenAI (design-rules §5c).** The Replicate models now support img2img: `Seedream 4.0/4.5` (up to 10/14 reference images), `Flux 2 Pro` (up to 8), `Wan 2.7` (up to 9), `Flux 1.1 Pro` + `Grok Imagine` (single reference). Only `Google Imagen 4` is text-to-image only (edit → clean **400**). The edit source-compatibility table + prose updated; **slow-model edits are ASYNC (202 + poll), same as generation** (only Grok Imagine edits synchronously) — noted in §5b. Docs only. |
| 2.27 | 2026-07-20 | **OpenAI image sources are async + edit-capable; only Grok Imagine is sync (design-rules §5b/§5c, product-creation-pipeline Phase 1).** The `OpenAI` source (now a modern gpt-image model) and `GPT Image 2` (now native gpt-image-2) BOTH ride the async 202+poll path and BOTH support img2img edit + multi-image — the docs previously listed OpenAI as a fast/200 model and GPT Image 2 as edit-`422`. Corrected: the ONLY fast synchronous model is `Grok Imagine`; every other source (Nano Banana default, OpenAI, Seedream 4.0/4.5, Flux 1.1 Pro, Flux 2 Pro, Google Imagen 4, Wan 2.7, GPT Image 2) returns **202** and must be polled. Edit-capable sources are now Nano Banana, OpenAI, and GPT Image 2. `ah_poll_generation` header note updated. Docs only. |
| 2.26 | 2026-07-18 | **Costs & margin pricing reference + polo crest placement note.** New `references/pricing.md`: per-variant `cost` lives on the **store-products list** (`GET /store/<store>/products` → `variants[].cost`) and is populated by the fulfillment sync — it is **absent from product-detail** and often **`null` in the catalog** (esp. Printify), the two places you'd first look. Price each variant to `cost / (1 - margin)` via per-variant `PUT` (the `/variants/bulk` endpoint is currently unreliable). `garment-catalog.md` gains a **"Polo crest placement"** note: a polo chest crest goes on the **left-chest** placement (`left_chest` / `chest_left_dtf` / `embroidery_chest_left`) inset to ~90%, NOT the `front` placement (a full torso print); Gelato polos render blank mockups. `SKILL.md` §3 decision tree + §5 updated. Docs only. |
| 2.25 | 2026-07-16 | **Set retail + shipping price when editing a draft order (orders-and-fulfillment §12).** The draft-order item edit now takes optional pricing: **`custom_price`** per item (per-unit retail price; omit → the variant's own price; for Printful/Gelato it flows to the provider draft), and order-level **`shipping_cost`** / **`tax`** (top-level on `POST`/`PUT /agents/v1/orders/<uuid>/items`) — the retail shipping/tax the customer pays, NOT the provider's cost; omit → the order's existing values are preserved; total re-prices as `subtotal + shipping_cost + tax`; negatives rejected. MCP tool `add_order_item` gains `custom_price` + `shipping_cost`. Docs only. |
| 2.24 | 2026-07-16 | **Edit a draft order's items before confirm (orders-and-fulfillment §12).** Add or remove items on an order while it's still a DRAFT (e.g. add another variant of the same product): `POST /agents/v1/orders/<uuid>/items` (add), `DELETE .../items/<item_id>` (remove), `PUT .../items` (replace) — MCP tools `add_order_item` / `remove_order_item`. Providers apply it automatically: Printful/Gelato edit the provider draft IN PLACE (same id); Printify has no edit API so it CANCELS + RE-CREATES the order (a new id — safe, drafts are uncharged). The order detail carries `supports_in_place_order_edit` so you can warn the user first; the edit result's `edit_method` is `in_place`/`recreated`/`local`. Errors: `409 order_not_editable` (already confirmed), `409 variant_not_synced`, `400 items_empty`. Docs only. |
| 2.23 | 2026-07-16 | **Aspect ratios / matching the print area (design-rules §5c, all-over-print §12).** Two ways to make a design's shape match the product's print area: (1) **generate at the aspect** — `POST /images/generate` `size` accepts `1024x1024` (square), `1024x1792` (tall/portrait), `1792x1024` (wide/landscape), so pick the one matching the product (tall for phone cases/posters, wide for banners/mugs/doormats, square for chest prints) up front; (2) **reshape an EXISTING design** — new `POST /images/generated/<uuid>/fit-aspect` endpoint (`{aspect:"W:H", mode:"pad"|"crop", background:"#RRGGBB"}`) pads (letterbox, keeps the whole design) or center-crops an existing image to a target ratio, returning a new gallery image. `fit-aspect` is **quota-free** (metered as storage, NOT an image generation), so prefer it over regenerating when only the shape is wrong; regenerate/outpaint only when you need new or extended content. `SKILL.md` §3 decision tree gains an aspect-ratio row. Docs only — no script, settings, or installer changes. |
| 2.22 | 2026-07-11 | **Wrap/multi-face placement is handled by the platform now (all-over-print §9/§10 rewrite).** Composition moved server-side (Garment Intelligence epic): `ship_product` / `create_product` / `prepare-print-data` with `print_style: "auto"` (the default) compose the correct per-placement print files on the platform — face windows, per-face rotation, solid structural panels, transparent per-face wraps, interior/label blanking, seam-avoidance, cylinder/dome insets — from a continuously-updated calibration set, so new garment fixes ship as platform DATA with no client update. The per-garment face-rect tables (socks, drawstring, wallet, duffle, backpack, mug, tote, notebook, bucket hat) are removed from the skill; the agent no longer hand-places geometry. The QUALITY GATES are unchanged and now framed as the agent's job: verify the mockup at full resolution — upright, no clipped lettering/subject, every placement covered, interior/label surfaces blank, structural panels a clean solid. Pairs with `apparelhub-mcp` v0.4.0. Docs only — no script, settings, or installer changes. |
| 2.21 | 2026-07-10 | **Prefer the ApparelHub MCP connector over the raw-HTTP pipeline (new SKILL.md §0).** When the skill and the hosted/custom MCP connector are both attached to a session, the skill's playbook used to steer the agent down its manual raw-API + bash-script pipeline (`ah_curl`, `make_transparent.py` chroma-keying, `ah_poll_mockup`, fetching mockup images by hand) even though the connector's tools were available — which is what strips leaves out of a design, clips lettering, and dead-ends on blocked image CDNs. New §0 (the first operational section) tells the agent: if connector tools like `design_apparel` / `ship_product` / `process_transparency` are present, call them directly and treat the rest of the skill as reference knowledge; the connector runs transparency, mockup rendering, variant resolution, and image hosting server-side. Everything from §1 (Authentication) on is now framed explicitly as the raw-HTTP fallback for runtimes with only an API key and no connector. Docs only — no script, settings, or installer changes. |
| 2.20 | 2026-07-10 | **Four more wrap-goods face rules (all-over-print §9).** From a proactive QC discovery sweep: (1) **tote bags** with a tall wrap area are front+back folded at the bottom — top-favor the visible front (like a drawstring bag). (2) **Softcover journals/notebooks** — the outside cover is back+spine+front laid flat; put the design on the FRONT cover (right half), clear of the spine crease. (3) **Bucket hats** — the front crown is a small tightly-curved dome; confine the design to the flat front-crown band or it wraps over the crown top and brim. (4) **Mugs/steins** get a TIGHTER front-arc inset (~44% width) than a tall bottle (~68%) — a mug's small diameter wraps more circumference, so a bottle-width inset wraps the design off the sides. Also: **interior/label surfaces print BLANK** — a journal's inside cover + pages and a reversible hat's inside faces are left unprinted, never solid-filled with the design background. Pairs with `apparelhub-mcp` v0.3.13. Docs only — no script, settings, or installer changes. |
| 2.19 | 2026-07-09 | **Seams must not split a subject (all-over-print §9, design-rules §7).** A backpack pocket seam (tote gusset, jacket zipper) cuts a full-face design in half. When the design has lettering or a subject (face, player's body, logo), favor the seam-free region so it stays intact — the SPAIN backpack split the goalkeeper and "La Roja" across the front-pocket seam; the design now sits in the upper-body window above it, with the lower front + pocket solid. Abstract patterns that read fine when split can span the seam. Pairs with `apparelhub-mcp` v0.3.11. Docs only — no script, settings, or installer changes. |
| 2.18 | 2026-07-09 | **Cylindrical drinkware inset (all-over-print §9, design-rules §7).** A water bottle / tumbler / mug / glass print area wraps AROUND the tube — top → shoulder, bottom → base, sides → around — so a design filling the area clips at every edge (the MOROCCO water bottle cut the flag star at the top and "MOROCCO" at the base). Inset the design into the flat frontal band (central ~68% width, y 0.13–0.85) with margin. Same "print area != visible face" family as the duffle and ear cups. Pairs with `apparelhub-mcp` v0.3.10. Docs only — no script, settings, or installer changes. |
| 2.17 | 2026-07-09 | **Low-res QC auto-recovers + reconciler pattern for scheduled builds (error-handling, product-creation-pipeline).** A design that keys/crops small (e.g. 847×396) no longer hard-blocks the QC gate and gets an item skipped forever — MCP v0.3.9 makes `process_transparency` upscale to a resolution floor and `verify_design_quality` treat low resolution as a WARN not a block, so the item builds; the warn just means "regenerate the source for sharp large-format detail." New `product-creation-pipeline.md` section: scheduled/unattended builds should be desired-state RECONCILERS — reconcile actual vs plan each run, never self-delete the trigger (deletion of a product is the rebuild signal, so fix+deploy then delete-to-rebuild recovers gaps), never STALL on one un-buildable item (report "pending", retry next run). Pairs with `apparelhub-mcp` v0.3.9. Docs only — no script, settings, or installer changes. |
| 2.16 | 2026-07-09 | **No clipped lettering + display-vs-structural face rule (all-over-print §9, design-rules §7).** Two QC refinements: (1) lettering must never be cut off by a print edge unless the designer asks — text sits fully inside the printable face with margin (the headphone ear cups clipped the "S" in SPAIN at the oval edge; now inset). (2) On multi-placement goods, DISPLAY faces (sock strips, both ear cups, wallet front+back, duffle front) get the design; STRUCTURAL panels (duffle sides/top/bottom/pocket, backpack top/bottom/pocket) get the solid background — never the design plastered full-bleed, never blank/white (the NORWAY duffle white strip). Every face covered, none blank, none busy/clipped. Physical hardware crossing a face (duffle strap, zipper) is not an edge clip. Pairs with `apparelhub-mcp` v0.3.8. Docs only — no script, settings, or installer changes. |
| 2.15 | 2026-07-09 | **No blank faces on multi-face/piece merch + design-resolution guidance (all-over-print §9/§11, design-rules §7).** Multi-FACE wraps get the design on EVERY face: zipper/passport wallets print on the front AND back exterior (a centered design splits at the spine — front = top half upright, back = bottom half rotated 180° past the fold); headphone ear cups both get the design (inset to the oval); duffles keep art in the central frontal window with no bare far side / white strip. Any product with more than one same-size placement (ear cups, two-panel goods) needs the design on every piece. New §11: designs shrink after keying/auto-crop (e.g. 847×596) and hard-block the platform's low-resolution QC gate — generate at the model's largest size, keep print files ≥~2000px, upscale to the print area when low (clears the gate), regenerate for real large-format detail, and DEFER (don't stall) an item that can't clear a genuine block. Mockup gate (§7/§8) gains blank-face and resolution checks. Pairs with `apparelhub-mcp` v0.3.7 (does all of this automatically). Docs only — no script, settings, or installer changes. |
| 2.14 | 2026-07-09 | **Wrap goods: the print area is not always the visible face (all-over-print §9-§10, design-rules §7, error-handling).** Grid-calibrated facts from the WC26 QC incidents: sublimation sock leg FRONT strips render the print file rotated 180° (file-top = toe) while the BACK strips render upright, the strip wraps the leg so only the central ~64% stays frontal, and ALL FOUR strips need files (unprinted = raw white fabric); drawstring-bag "front" areas are front+back in ONE file folded at the bottom (visible front = top ~50%, art centered on the area straddles the fold); AOP-backpack sibling placements (top/bottom/pocket) need solid background files. Extreme-aspect fill areas (≤1:2 / ≥2.2:1) are suspect wraps — preview before building. Mockup gate (§7) gains upright-orientation, fold/silhouette-clipping, all-placements-covered, no-chroma-green, and full-resolution-inspection checks, plus: collar breathing room is an APPAREL concept — non-apparel placed goods (phone cases, mugs) center the design. Error-handling gains the single-dimension variant case (clear phone cases have NO color dimension — match by size or pass provider_variant_ids). Pairs with `apparelhub-mcp` v0.3.6 (does all of this automatically). Docs only — no script, settings, or installer changes. |
| 2.13 | 2026-07-09 | **Mockups must cover every color variant you import (design-rules §7b).** New rule: decide your color variants BEFORE generating the mockup, then make sure the mockup covers each color. Mockups render per variant and same-color variants share one print image, so "the first N variants" is all ONE color — leaving the other imported colors (e.g. White when you offer Black + White) with no mockup and a wrong-looking gallery. `ship_product` now renders **one mockup per distinct imported color** automatically (`apparelhub-mcp` v0.3.3); `create_product`'s auto-mockup samples one-per-color from the catalog (best-effort — it runs before variants exist, so for color-accurate mockups use `ship_product` or pass `mockup_variant_ids`); raw Phase 3 should pass one `variant_id` per color. Docs only — no script, settings, or installer changes. |
| 2.12 | 2026-07-08 | **Product pipeline: "map to store" is NOT "publish to a sales channel", + mockup/variant automation guardrails.** Reframes the creation pipeline so it ENDS at Phase 7 (product created, mapped to the store, fulfillment-synced = sellable); pushing a storefront listing is a new OPT-IN Phase 8, done ONLY when the user explicitly asks to list/publish (a scheduled task had wrongly turned "map it to the store" into a WooCommerce sync). `SKILL.md` §2 + `references/product-creation-pipeline.md` gain a prominent "map ≠ publish" callout. Phase 5 now says read the garment's REAL colors/sizes from `get_garment_details` — never hard-code `S/M/L/XL/2XL` (one-size caps/beanies/accessories otherwise resolve to 0 variants). Phase 3 stresses a finished product must show a garment mockup, not the raw design. Recommends `ship_product` for automated/scheduled runs (one call, correct order) and adds the MCP-tool→phase mapping (`sync_to_fulfillment` = map-to-store; `sync_to_channel` = opt-in Phase 8). `references/error-handling.md` gains `product not associated with store`, `0 variants added (wrong sizes)`, and a `generate_mockup` no-op note. Pairs with `apparelhub-mcp` v0.3.1. Docs only — no script, settings, or installer changes. |
| 2.11 | 2026-07-08 | **Post-sale fulfillment issues (the platform backend epic #510).** New `references/fulfillment-issues.md` documents the issue-reporting loop: report a defect on an order (`POST /agents/v1/orders/{uuid}/issues` with category/items/resolution), attach evidence (direct multipart up to 4MB or the presigned initiate/complete flow; 5 files max, one video, mirroring provider limits), get the provider-ready problem-report summary + dashboard deep-link (`GET .../provider-report`), mark it filed with the claim reference (`POST .../submit-upstream`), track and resolve (`POST .../resolve`), and create a zero-charge replacement order (`POST .../replacement-order` with structured `recipient_unavailable`/`variant_unlinked` failures). Teaches the three facts agents must relay: providers accept problem reports ONLY in their own dashboards, the window is 30 days FROM DELIVERY (`eligibility.days_remaining`), and change-of-mind (size/color preference) is not claim-eligible. Printful refunds auto-resolve filed issues; Printify resolves manually. `SKILL.md` (§3 decision tree) references it. Docs only — no script, settings, or installer changes. |
| 2.10 | 2026-07-08 | **Analytics endpoints (the platform backend epic #387).** New `references/analytics.md` documents the six read-only `/agents/v1/analytics/*` endpoints so an agent can pull metrics programmatically: `summary` (KPIs + prior-period deltas), `timeseries` (day/week/month), `breakdown` (`dimension` = product_type/sales_channel/fulfillment_provider/product/variant/hold_reason), `ops` (velocity + hold/cancel/refund rates), `export` (CSV, `report` = summary/timeseries/breakdown/ops/orders), and `portfolio` (per-client/per-workspace roll-up, agency-Enterprise only). Teaches the three semantics agents miss: the `advanced_analytics` tier gate (`403 feature_unavailable`, Professional+; `portfolio` additionally `403`s `client_portfolio` for non-agency accounts), currencies segmented never summed (read `currency` + `currencies_present`), and margins only over cost-known orders (`margin_coverage`). Shared params `start`/`end`/`store`/`currency`/`?workspace=`. `SKILL.md` (§3 decision tree) and `references/api-contract.md` (§4 catalog) reference it. Full schemas in the served OpenAPI spec (Analytics tag). Docs only — no script, settings, or installer changes. |
| 2.9 | 2026-06-30 | **Multi-workspace key scope (an internal platform ticket).** An Agent API key can now be scoped to one OR MORE workspaces (each with a role), so the `GET /agents/v1/workspaces` `key_scope` field changed shape: it is now `{pinned, workspaces:[{workspace_uuid, role}]}` (was `{pinned, workspace_uuid, role}`). For a scoped key, `key_scope.workspaces` lists exactly the workspaces it can act in, each with the key's role; `?workspace=` works for any of them and rejects the rest with `403 workspace_forbidden`. Account-wide keys are unchanged (`pinned:false`, empty `workspaces`). `references/workspaces.md` (§2 + §5), `SKILL.md` (§6), `references/api-contract.md` (§4b), and `references/error-handling.md` updated. Docs only — no script, settings, or installer changes. |
| 2.8 | 2026-06-30 | **Workspace discovery route (an internal platform epic) + example scrub.** Documents the new `GET /agents/v1/workspaces` route — the piece v2.6 was missing. v2.6 told the agent "there's no agent endpoint that lists an account's workspaces," so an agent asked to scope to a workspace by name had no way to resolve the name to a `uuid` and choked. Now `GET /agents/v1/workspaces` returns `{workspaces:[{uuid,name,is_default}], active_workspace, key_scope}`; the agent matches the user's name to a `uuid`, then scopes with `?workspace=<uuid>`. A workspace-pinned key lists only its own workspace. `references/workspaces.md` (§2) now leads with the discovery route, and `SKILL.md` (§6) + `references/api-contract.md` (§4b) reference it. Also scrubbed every example and doc of real account-specific values (provider UUIDs, store URLs, workspace/client names) in favor of generic placeholders. Docs only — no script, settings, or installer changes. |
| 2.7 | 2026-06-30 | **Agent 402 account-suspended contract (an internal platform epic).** `references/error-handling.md` documents the read-only-suspension 402: a new status-table row + a dedicated "Account suspended (402)" section. When an account's invite-only trial expires with no card on file, the account goes read-only — reads still succeed but writes / quota / sync return `402 {error:"account_suspended", reason:"trial_expired", tier, message, billing_url}`. The agent can't pay; it should stop the write (don't retry), tell the account OWNER to add a payment method at the `billing_url`, and note that everything is safe and held storefront orders auto-release on conversion. Docs only — no script, settings, or installer changes. |
| 2.6 | 2026-06-30 | **Workspace scoping docs (enterprise accounts).** New `references/workspaces.md` plus a `SKILL.md` "Workspaces" section, for issue #25: on Enterprise / agency accounts every Agent API call acts within one **active workspace**. With no param it's the **Default** workspace; `?workspace=<uuid>` targets a specific one (unknown uuid → `404 workspace_not_found`, real-but-inaccessible → `403 workspace_forbidden`). Products and generated images carry a `workspaces` array, stores carry `workspace_uuid` / `workspace_name` (Model A visibility), and workspace-scoped API keys pin a workspace + role (a role lacking `design.generate` gets `403 forbidden` on image gen). The point: an agent on an enterprise account knows how to target a workspace and won't misread a workspace-scoped list as "missing data." `api-contract.md` (§4b + status table) and `error-handling.md` (§2b) gain the `?workspace=` param and the two error codes. Docs only — no script, settings, or installer changes. Refs an internal platform ticket, #377. |
| 2.5 | 2026-06-23 | **Async image-gen polling + security nits.** New `ah_poll_generation` script + docs (issue #20): `POST /images/generate` returns **202 + `image_uuid`** for slow models (the Nano Banana default, Seedream 4.0/4.5, Flux 2 Pro, Google Imagen 4, Wan 2.7, GPT Image 2), so the agent must poll `GET /images/upload/<uuid>/status` until `processing_status` is completed (read `url`) or failed (read `error`). Fast models (OpenAI, Grok Imagine, Flux 1.1 Pro) still return 200 + url. Security hardening (issue #17): `ah_check` now feeds the API key through a curl stdin config instead of `-H`, so the key stays out of process argv (SKILL.md documents the same pattern for shared machines); removed the broad `Bash(python3 /tmp/*)` grant from `settings.recommended.json` (`make_transparent.py` keeps its own scoped grant); raw object-storage hostnames swapped to `https://cdn.apparelhub.ai` in the porting guide and the curl allowlist. **Migration:** if you previously merged `settings.recommended.json`, remove the `Bash(python3 /tmp/*)` line, add the two `ah_poll_generation` grants, and replace the `*.s3.amazonaws.com` curl patterns with the `cdn.apparelhub.ai` ones. |
| 2.4 | 2026-06-23 | **Multi-channel repositioning + reconcile docs.** The descriptor and identity lines (README, `SKILL.md` frontmatter + intro, `BOOTSTRAP-PROMPT.md`) now describe ApparelHub as a multi-channel ecommerce platform for custom merchandise instead of a print-on-demand platform. Channel lists (Shopify/Etsy/WooCommerce/Wix) and the Printful/Printify fulfillment copy are unchanged. Plus `references/orders-and-fulfillment.md` gains section 11: how an agent reconciles a sales-channel order's payment, refund, cancellation, and fulfillment status with the channel (epic an internal platform ticket Phase 5). |
| 2.3 | 2026-06-18 | **Printify embroidery.** `references/embroidery.md` gains a full PRINTIFY EMBROIDERY section (P1-P8). The 15-thread palette is identical hex to Printful (Madeira codes added), but Printify auto-digitizes so you send NO thread colors (unlike Printful's `thread_colors_*` options); 6-color cap; up to 36h post-order digitization; embroidery area sizes; the negative-space-fill and outline-for-solid-backgrounds rules; pre-digitized Embroidery Ready Fonts. Plus the dual-decoration mockup limitation: on a blueprint whose provider does embroidery and DTF, Printify generates ONLY the embroidery mockup scene, so a DTF crest prints correctly but cannot be previewed (verified, and reproduced in Printify's own designer). |
| 2.2 | 2026-06-17 | **Agent order management.** `references/orders-and-fulfillment.md` gains sections 8-10 plus a new `examples/order-management.md`: the per-store fulfillment workflow (auto / confirm / review), approval authority (you / your AI agent / smart rules), smart guardrails (high-value, low-margin, negative-margin), the agent approval queue (`GET /agents/v1/orders?requires_approval=true`), the two distinct holds (ApparelHub approval vs Printful design), and the opt-in HMAC-signed `order.awaiting_approval` callback. |
| 2.1 | 2026-06-11 | Docs reference the branded `cdn.apparelhub.ai` host instead of the raw object-storage URL, matching the platform cutover. Worked-example response shapes and verification `curl`s now show CDN URLs (both forms serve the same bytes). |
| 2.0 | 2026-06-06 | **Platform-neutral, security-first re-architecture.** `ah_curl` deleted (forwarded keys to arbitrary URLs). `APPARELHUB_API_BASE` removed from user path; canonical host hard-pinned in every helper. Installer no longer persists the API key to disk or edits shell rc by default (opt in with `--persist`). New top-level `SECURITY.md` documents trust model + non-goals + threat model, enforced by CI grep. New `apparelhub/references/api-contract.md` makes the HTTP contract consumable by bare-HTTP agents. New `porting-guides/` directory ships per-platform walkthroughs for bare-HTTP, Claude Code, and ChatGPT/Gemini tool-calling. Claude-Code-specific bits moved to a `claude-code/` overlay. All "exists to suppress the permission prompt" framing removed from scripts and docs; see SECURITY.md §2a. |
| 1.16 | 2026-06-05 | Rebrand bridge → harness, drop Docker mentions, strip em-dashes from all public-facing surfaces. |
| 1.15 | 2026-06-05 | End-to-end environment selection via `APPARELHUB_API_BASE` (removed in v2.0). |
| 1.14 | 2026-06-05 | Bridge install prompt rewritten to be self-contained. |
| 1.13 | 2026-06-05 | Multi-harness install support; universal bootstrap prompt for non-Claude-Code agents. |
| 1.12 | 2026-06-04 | Initial public skill funnel release (epic-agent-onboarding). |
| 1.11 and earlier | — | See GitHub Releases page for full history. |

---

## Contributing

Bug reports and PRs welcome at <https://github.com/ApparelHub-AI/apparelhub-skills/issues>.
Security issues go to `security@apparelhub.ai` per [`SECURITY.md`](./SECURITY.md) §4.

The acceptance playbook in [`acceptance/playbook.md`](./acceptance/playbook.md)
is run before every release tag. The CI workflow in
[`.github/workflows/forbidden-patterns.yml`](./.github/workflows/forbidden-patterns.yml)
guards against v1.x antipatterns reappearing.

---

## License

This project is licensed under the [MIT License](./LICENSE).
